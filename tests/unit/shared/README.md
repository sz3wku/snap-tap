# Core Unit Tests

Tests for `src/snap_tap`: driver contracts, snapshots, semantics, targets,
primitives, receipts, and low-level evidence.

Core tests should use fake driver output and fixtures, not a real phone.

